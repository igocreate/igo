{
  "name": "{project.name}",
  "version": "0.0.1",
  "description": "",
  "main": "app.js",
  "scripts": {
    "jshint": "jshint --reporter=node_modules/jshint-stylish ./app/**/*.js ./locales/**/*.json || true",
    "nodemon": "nodemon app.js",
    "start": "npm-run-all --parallel nodemon webpack",
    "webpack": "webpack -p --progress --watch",
    "test": "mocha"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.0.0-alpha.6",
    "igo": "{igo.version}",
    "font-awesome": "^4.7.0",
    "jquery": "^3.1.1",
    "tether": "^1.4.0"
  },
  "devDependencies": {
    "igo-dev": "{igo-dev.version}"
  }
}
