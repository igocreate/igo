# pnpm blocks postinstall scripts unless a package is listed here. These two
# compile or download a native binary, which they need to run at all.
allowBuilds:
  esbuild: true
  '@parcel/watcher': true
