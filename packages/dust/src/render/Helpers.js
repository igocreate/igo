// User helpers registered via `dust.helpers.NAME = fn` end up here.
// Built-ins (eq / ne / lt / lte / gt / gte / first / last / sep /
// select / none / any) are inlined at compile time — see
// src/compile/BuiltinHelpers.js.
module.exports = {};
